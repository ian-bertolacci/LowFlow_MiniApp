cmake_minimum_required(VERSION 3.5)
include(${CMAKE_DIR}/utilities.txt)
include(${CMAKE_DIR}/build_variant.txt)

# Variant name
set(VARIANT_NAME simple_kokkos)
# List source files
set(VARIANT_SOURCE_FILES variant_types.cpp variant_science.cpp variant_metrics.cpp variant_configure.cpp variant_setup_teardown.cpp)
# List libraries
set(VARIANT_LINK_LIBRARIES kokkos dl pthread )

CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_SERIAL")
CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_SERIAL;ENABLE_VARIANT_METRICS")
CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_OPENMP")
CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_OPENMP;ENABLE_VARIANT_METRICS")
CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_THREADS")
CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_THREADS;ENABLE_VARIANT_METRICS")
# CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_CUDA")
# CREATE_VARIANT_TARGET(${VARIANT} "${BASE_SOURCE_FILES}" "${VARIANT_SOURCE_FILES}" ${INSTALL_BIN_DIR} "${VARIANT_LINK_LIBRARIES}" "USE_CUDA;ENABLE_VARIANT_METRICS")
